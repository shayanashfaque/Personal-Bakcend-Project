<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Footlinks - Add Court</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background: #f8f9fa;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }
    .court-card {
      background: #fff;
      padding: 2rem;
      border-radius: 10px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      width: 100%;
      max-width: 420px;
    }
    h2 {
      text-align: center;
      margin-bottom: 1.5rem;
      color: #0a6847;
    }
    label {
      display: block;
      margin-bottom: 0.3rem;
      font-size: 0.9rem;
      color: #333;
    }
    input {
      width: 100%;
      padding: 0.7rem;
      margin-bottom: 1rem;
      border: 1px solid #ccc;
      border-radius: 6px;
      font-size: 1rem;
    }
    input:focus {
      outline: none;
      border-color: #0a6847;
      box-shadow: 0 0 4px rgba(10,104,71,0.4);
    }
    button {
      width: 100%;
      padding: 0.8rem;
      background: #0a6847;
      border: none;
      border-radius: 6px;
      color: #fff;
      font-size: 1rem;
      cursor: pointer;
      margin-top: 0.5rem;
    }
    button:hover {
      background: #085c3f;
    }
    .extra-links {
      text-align: center;
      margin-top: 1rem;
      font-size: 0.9rem;
    }
    .extra-links a {
      color: #0a6847;
      text-decoration: none;
    }
    .extra-links a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="court-card">
    <h2>Add New Court</h2>
    <form action="/courts/new" method="POST">
      <label for="name">Court Name</label>
      <input type="text" id="name" name="name" required>

      <label for="location">Location</label>
      <input type="text" id="location" name="location" required>

      <label for="pricePerHour">Price Per Hour</label>
      <input type="number" id="pricePerHour" name="pricePerHour" required min="0">

      <button type="submit">Add Court</button>
    </form>
    <div class="extra-links">
      <p><a href="/courts">Back to Courts</a></p>
    </div>
  </div>
</body>
</html>

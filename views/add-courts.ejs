<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Footlinks - Add Court</title>
  <link rel="stylesheet" href="/style.css" />
  <style>
    body {
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      background: #f8f9fa;
    }
    .court-card {
      background: #fff;
      padding: 2rem;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      width: 100%;
      max-width: 500px;
      text-align: center;
    }
    .court-card h2 {
      margin-bottom: 1.5rem;
      color: #0a6847;
    }
    .form-group {
      margin-bottom: 1rem;
      text-align: left;
    }
    .form-group label {
      display: block;
      margin-bottom: 0.5rem;
      font-size: 0.9rem;
      color: #333;
    }
    .form-group input, .form-group textarea {
      width: 100%;
      padding: 0.75rem;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 1rem;
    }
    .form-group textarea {
      resize: vertical;
      min-height: 80px;
    }
    .form-group input:focus, .form-group textarea:focus {
      outline: none;
      border-color: #0a6847;
      box-shadow: 0 0 4px rgba(10,104,71,0.4);
    }
    .submit-btn {
      width: 100%;
      padding: 0.75rem;
      background: #0a6847;
      color: #fff;
      border: none;
      border-radius: 8px;
      font-size: 1rem;
      cursor: pointer;
      margin-top: 1rem;
      transition: 0.2s;
    }
    .submit-btn:hover {
      background: #085c3f;
    }
  </style>
</head>
<body>
  <div class="court-card">
    <h2>Add a New Football Court</h2>
    <form action="/add-court" method="POST">
      <div class="form-group">
        <label for="name">Court Name</label>
        <input type="text" id="name" name="name" placeholder="Greenfield Arena" required />
      </div>
      <div class="form-group">
        <label for="location">Location</label>
        <input type="text" id="location" name="location" placeholder="Downtown City" required />
      </div>
      <div class="form-group">
        <label for="price">Price (per hour)</label>
        <input type="text" id="price" name="price" placeholder="$40/hr" required />
      </div>
      <div class="form-group">
        <label for="img">Image URL</label>

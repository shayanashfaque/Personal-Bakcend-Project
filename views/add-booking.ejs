<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Booking - Footlinks</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f8f9fa;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      min-height: 100vh;
      margin: 0;
    }
    .form-container {
      background: #fff;
      margin-top: 3rem;
      padding: 2rem;
      border-radius: 10px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      width: 100%;
      max-width: 500px;
    }
    h2 {
      text-align: center;
      margin-bottom: 1.5rem;
      color: #0a6847;
    }
    label {
      display: block;
      margin: 0.5rem 0 0.3rem;
      font-weight: bold;
    }
    input, select, button {
      width: 100%;
      padding: 0.6rem;
      margin-bottom: 1rem;
      border: 1px solid #ccc;
      border-radius: 6px;
    }
    button {
      background: #0a6847;
      color: #fff;
      border: none;
      font-size: 1rem;
      cursor: pointer;
    }
    button:hover {
      background: #085c3f;
    }
    .info-text {
      margin-bottom: 1rem;
      color: #333;
    }
  </style>
</head>
<body>
  <div class="form-container">
    <h2>Add Booking</h2>

    <form action="/booking/new" method="POST">
      <!-- Hidden IDs -->
      <input type="hidden" name="court" value="<%= court._id %>">
      <input type="hidden" name="user" value="<%= user._id %>">

      <!-- Info Display -->
      <p class="info-text">Booking court: <strong><%= court.name %></strong></p>
      <p class="info-text">User: <strong><%= user._id %></strong></p>

      <!-- Booking Fields -->
      <label>Date</label>
      <input type="date" name="date" required>

      <label>Start Time</label>
      <input type="time" name="startTime" required>

      <label>End Time</label>
      <input type="time" name="endTime" required>

      <button type="submit">Book Now</button>
    </form>
  </div>
</body>
</html>

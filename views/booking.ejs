<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>My Current Bookings - Footlinks</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f8f9fa;
      padding: 2rem;
      margin: 0;
    }

    h1 {
      text-align: center;
      color: #0a6847;
      margin-bottom: 2rem;
    }

    .booking {
      background: white;
      border-radius: 12px;
      padding: 1.2rem 1.5rem;
      margin: 1rem auto;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      max-width: 750px;
      line-height: 1.6;
    }

    .booking h3 {
      margin-top: 0;
      color: #0a6847;
      font-size: 1.3rem;
    }

    .booking p {
      margin: 0.4rem 0;
      color: #333;
    }

    .cancel-btn, .receipt-btn {
      background-color: #0a6847;
      color: white;
      border: none;
      padding: 0.5rem 1rem;
      border-radius: 6px;
      cursor: pointer;
      font-size: 0.95rem;
      margin-top: 0.8rem;
      margin-right: 0.5rem;
      transition: background-color 0.2s ease;
    }

    .cancel-btn:hover { background-color: #085c3f; }
    .receipt-btn:hover { background-color: #085c3f; }

    .no-bookings {
      text-align: center;
      color: #666;
      margin-top: 3rem;
    }
  </style>
</head>
<body>
  <h1>My Current Bookings</h1>

  <% if (!bookings || bookings.length === 0) { %>
    <p class="no-bookings">You have no active bookings.</p>
  <% } else { %>
    <% bookings.forEach(b => { %>
      <div class="booking">
        <h3><%= b.court ? b.court.name : "Unknown Court" %></h3>
        <p><%= b.court ? b.court.location : "Location unavailable" %></p>
        <% if (b.slot) { %>
          <p>
            Date: <%= new Date(b.slot.date).toLocaleDateString() %> |
            Time: <%= b.slot.startTime %> - <%= b.slot.endTime %> |
            Price: $<%= b.slot.price || 'N/A' %>
          </p>
        <% } else { %>
          <p><em>Slot details unavailable.</em></p>
        <% } %>

        <!-- View Receipt button -->
        <a href="/receipt/<%= b._id %>" class="receipt-btn">View Receipt</a>

        <!-- Cancel button -->
        <form action="/bookings/<%= b._id %>/cancel" method="POST" style="display: inline;">
          <button type="submit" class="cancel-btn" onclick="return confirm('Are you sure you want to cancel this booking?')">Cancel Booking</button>
        </form>
      </div>
    <% }) %>
  <% } %>
</body>
</html>
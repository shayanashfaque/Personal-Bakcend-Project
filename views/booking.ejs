<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>My Bookings | Footlinks</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f8f9fa;
      margin: 0;
      padding: 2rem;
    }

    h1 {
      text-align: center;
      color: #0a6847;
      margin-bottom: 2rem;
    }

    .bookings-container {
      max-width: 900px;
      margin: 0 auto;
      background: #fff;
      padding: 2rem;
      border-radius: 16px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
    }

    .no-bookings {
      text-align: center;
      color: #777;
      font-size: 1.1rem;
      padding: 2rem 0;
    }

    .booking-card {
      display: flex;
      flex-direction: column;
      border-bottom: 1px solid #ddd;
      padding: 1rem 0;
    }

    .booking-card:last-child {
      border-bottom: none;
    }

    .booking-card h3 {
      margin: 0;
      color: #0a6847;
      font-size: 1.2rem;
    }

    .booking-info {
      color: #333;
      margin: 0.3rem 0;
      font-size: 0.95rem;
    }

    .booking-actions {
      margin-top: 0.5rem;
    }

    .receipt-link {
      background: #0a6847;
      color: white;
      text-decoration: none;
      padding: 0.4rem 0.8rem;
      border-radius: 6px;
      font-weight: 500;
      transition: background 0.2s ease;
    }

    .receipt-link:hover {
      background: #085c3f;
    }
  </style>
</head>
<body>
  <h1>My Bookings</h1>

  <div class="bookings-container">
    <% if (bookings.length === 0) { %>
      <p class="no-bookings">You have no bookings yet.</p>
    <% } else { %>
      <% bookings.forEach(b => { %>
        <div class="booking-card">
          <h3><%= b.court.name %></h3>
          <p class="booking-info">
            📍 <%= b.court.location %><br>
            ⏰ <%= b.startTime %> - <%= b.endTime %><br>
            📅 <%= b.date ? new Date(b.date).toLocaleDateString() : "N/A" %>
          </p>
          <div class="booking-actions">
            <a href="/receipt/<%= b._id %>" class="receipt-link">View Receipt</a>
          </div>
        </div>
      <% }) %>
    <% } %>
  </div>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Footlinks - Bookings</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 2rem;
      background: #f8f9fa;
    }
    h2 {
      text-align: center;
      margin-bottom: 2rem;
      color: #0a6847;
    }
    .booking-list {
      display: flex;
      flex-direction: column;
      gap: 1rem;
      max-width: 800px;
      margin: 0 auto;
    }
    .booking-card {
      background: #fff;
      padding: 1.5rem;
      border-radius: 12px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .booking-info {
      font-size: 0.95rem;
    }
    .booking-info strong {
      color: #0a6847;
    }
    .empty {
      text-align: center;
      color: #666;
      font-size: 1rem;
      margin-top: 2rem;
    }
    .add-btn {
      display: inline-block;
      margin: 2rem auto;
      padding: 0.75rem 1.5rem;
      background: #0a6847;
      color: #fff;
      text-decoration: none;
      border-radius: 8px;
      font-size: 1rem;
      transition: 0.2s;
    }
    .add-btn:hover {
      background: #085c3f;
    }
  </style>
</head>
<body>
  <h2>My Bookings</h2>

  <% if (bookings.length > 0) { %>
    <div class="booking-list">
      <% bookings.forEach(booking => { %>
        <div class="booking-card">
          <div class="booking-info">
            <p><strong>Court:</strong> <%= booking.court.name %></p>
            <p><strong>Date:</strong> <%= booking.date.toDateString() %></p>
            <p><strong>Time:</strong> <%= booking.startTime %> - <%= booking.endTime %></p>
            <p><strong>User:</strong> <%= booking.user.name %></p>
          </div>
        </div>
      <% }) %>
    </div>
  <% } else { %>
    <p class="empty">No bookings yet.</p>
  <% } %>

  <div style="text-align:center;">
    <a href="/booking/new" class="add-btn">+ Add Booking</a>
  </div>
</body>
</html>

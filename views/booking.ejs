<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Footlinks - My Bookings</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background: #f8f9fa;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      min-height: 100vh;
    }
    .booking-list {
      background: #fff;
      margin-top: 3rem;
      padding: 2rem;
      border-radius: 10px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
      width: 100%;
      max-width: 700px;
    }
    h2 {
      text-align: center;
      margin-bottom: 1.5rem;
      color: #0a6847;
    }
    .booking-item {
      border: 1px solid #ddd;
      border-radius: 6px;
      padding: 1rem;
      margin-bottom: 1rem;
      background: #fdfdfd;
    }
    .booking-item h3 {
      margin: 0;
      font-size: 1.2rem;
      color: #0a6847;
    }
    .booking-item p {
      margin: 0.3rem 0;
      color: #333;
    }
    .extra-links {
      text-align: center;
      margin-top: 1rem;
      font-size: 0.9rem;
    }
    .extra-links a {
      color: #0a6847;
      text-decoration: none;
    }
    .extra-links a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="booking-list">
    <h2>My Bookings</h2>

    <% if (bookings.length === 0) { %>
      <p style="text-align:center; color:#555;">No bookings yet. <a href="/courts">Book a court now</a>.</p>
    <% } else { %>
      <% bookings.forEach(booking => { %>
        <div class="booking-item">
          <h3><%= booking.court.name %></h3>
          <p><strong>Date:</strong> <%= booking.date.toDateString() %></p>
          <p><strong>Time:</strong> <%= booking.startTime %> - <%= booking.endTime %></p>
          <p><strong>Location:</strong> <%= booking.court.location %></p>
        </div>
      <% }) %>
    <% } %>

    <div class="extra-links">
      <p><a href="/courts">Back to Courts</a></p>
    </div>
  </div>
</body>
</html>

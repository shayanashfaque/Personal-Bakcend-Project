<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Footlinks - Courts</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 2rem;
      background: #f8f9fa;
    }
    h1 {
      text-align: center;
      margin-bottom: 2rem;
      color: #0a6847;
    }
    .court-list {
      display: flex;
      flex-direction: column;
      gap: 1.5rem;
      max-width: 600px;
      margin: 0 auto;
    }
    .court-card {
      background: #fff;
      padding: 1.5rem;
      border-radius: 10px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }
    .court-card h2 {
      margin: 0;
      color: #333;
    }
    .court-card p {
      margin: 0.5rem 0 1rem;
      color: #555;
    }
    .book-btn {
      display: inline-block;
      padding: 0.6rem 1.2rem;
      background: #0a6847;
      color: #fff;
      text-decoration: none;
      border-radius: 6px;
      transition: 0.2s;
    }
    .book-btn:hover {
      background: #085c3f;
    }
  </style>
</head>
<body>
  <h1>Available Courts</h1>

  <div class="court-list">
    <% courts.forEach(court => { %>
      <div class="court-card">
        <h2><%= court.name %> - $<%= court.pricePerHour %>/hour</h2>
        <p><%= court.location %></p>
        <!-- Use param instead of query -->
        <a class="book-btn" href="/booking/new/<%= court._id %>">Book Now</a>
      </div>
    <% }) %>
  </div>
</body>
</html>

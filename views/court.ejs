<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Footlinks - Courts</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 2rem;
      background: #f8f9fa;
    }
    h2 {
      text-align: center;
      margin-bottom: 2rem;
      color: #0a6847;
    }
    .court-list {
      display: flex;
      flex-direction: column;
      gap: 1rem;
      max-width: 900px;
      margin: 0 auto;
    }
    .court-card {
      display: flex;
      background: #fff;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 2px 6px rgba(0,0,0,0.1);
      transition: transform 0.2s;
    }
    .court-card:hover {
      transform: translateY(-3px);
    }
    .court-card img {
      width: 200px;
      height: 150px;
      object-fit: cover;
    }
    .court-info {
      flex: 1;
      padding: 1rem;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
    }
    .court-info h3 {
      margin: 0;
      font-size: 1.2rem;
      color: #0a6847;
    }
    .court-info p {
      margin: 0.3rem 0;
      font-size: 0.95rem;
      color: #444;
    }
    .book-btn {
      display: inline-block;
      margin-top: 0.5rem;
      padding: 0.6rem 1.2rem;
      background: #0a6847;
      color: #fff;
      text-decoration: none;
      border-radius: 8px;
      font-size: 0.9rem;
      transition: background 0.2s;
      width: fit-content;
    }
    .book-btn:hover {
      background: #085c3f;
    }
    .empty {
      text-align: center;
      color: #666;
      margin-top: 2rem;
    }
  </style>
</head>
<body>
  <h2>Available Courts</h2>

  <% if (courts.length > 0) { %>
    <div class="court-list">
      <% courts.forEach(court => { %>
        <div class="court-card">
          <img src="<%= court.img || '/images/default-court.jpg' %>" alt="<%= court.name %>">
          <div class="court-info">
            <div>
              <h3><%= court.name %> - $<%= court.pricePerHour %>/hour</h3>
              <p><%= court.location %></p>
            </div>
            <a href="/booking/new?courtId=<%= court._id %>" class="book-btn">Book Now</a>
          </div>
        </div>
      <% }) %>
    </div>
  <% } else { %>
    <p class="empty">No courts available yet.</p>
  <% } %>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Courts - Footlinks</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background: #f8f9fa;
      margin: 0;
      padding: 2rem;
    }

    h1 {
      text-align: center;
      color: #0a6847;
      margin-bottom: 2rem;
    }

    .courts-container {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 1.5rem;
      max-width: 1200px;
      margin: auto;
    }

    .court-card {
      background: #fff;
      border-radius: 16px;
      overflow: hidden;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      text-decoration: none;
      color: inherit;
      transition: transform 0.2s ease, box-shadow 0.2s ease;
      display: flex;
      flex-direction: column;
    }

    .court-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 6px 14px rgba(0,0,0,0.15);
    }

    .court-image {
      width: 100%;
      height: 180px;
      object-fit: cover;
    }

    .court-content {
      padding: 1rem;
    }

    .court-content h3 {
      margin: 0.5rem 0;
      color: #0a6847;
    }

    .court-content p {
      margin: 0.3rem 0;
      color: #333;
    }

    .book-btn {
      display: inline-block;
      margin-top: 0.8rem;
      background: #0a6847;
      color: #fff;
      text-decoration: none;
      padding: 0.5rem 1rem;
      border-radius: 8px;
      font-weight: 500;
      transition: background 0.2s ease;
    }

    .book-btn:hover {
      background: #085c3f;
    }

    .no-courts {
      text-align: center;
      color: #777;
      margin-top: 3rem;
    }
  </style>
</head>
<body>
  <h1>Available Courts</h1>

  <div class="courts-container">
    <% if (courts.length === 0) { %>
  <p class="no-courts">No courts found</p>
<% } else { %>
  <% courts.forEach(court => { %>
    <a href="/courts/<%= court._id %>" class="court-card">
      <img src="<%= (court.images && court.images.length > 0) ? court.images[0] : 'https://via.placeholder.com/300x180?text=No+Image' %>"
           alt="<%= court.name %>" class="court-image">

      <div class="court-content">
        <h3><%= court.name %></h3>
        <p><%= court.location %></p>
        <p>$<%= court.pricePerHour %> per hour</p>

        <% if (court.averageRating) { %>
          <p>⭐ <%= court.averageRating.toFixed(1) %> / 5</p>
        <% } else { %>
          <p>⭐ Not yet rated</p>
        <% } %>

        <a href="/courts/<%= court._id %>" class="book-btn">View Details</a>
      </div>
    </a>
  <% }) %>
<% } %>

 
</body>
</html>

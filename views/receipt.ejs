<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Booking Receipt | Footlinks</title>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f8f9fa;
      margin: 0;
      padding: 2rem;
    }

    .receipt-container {
      max-width: 600px;
      margin: 2rem auto;
      background: #fff;
      padding: 2rem;
      border-radius: 16px;
      box-shadow: 0 4px 10px rgba(0,0,0,0.1);
      text-align: center;
    }

    h1 {
      color: #0a6847;
      margin-bottom: 1rem;
    }

    h3 {
      margin-top: 1.5rem;
      color: #333;
    }

    .info {
      text-align: left;
      margin: 1.5rem 0;
      font-size: 1rem;
      color: #444;
      line-height: 1.6;
    }

    .info strong {
      color: #0a6847;
    }

    .divider {
      border: none;
      height: 1px;
      background-color: #ddd;
      margin: 1.5rem 0;
    }

    .status {
      background: #e8f5e9;
      color: #0a6847;
      padding: 0.7rem 1rem;
      border-radius: 8px;
      display: inline-block;
      font-weight: 600;
      margin-top: 1rem;
    }

    .booking-id {
      font-size: 0.9rem;
      color: #666;
      margin-top: 0.5rem;
    }

    .btn {
      display: inline-block;
      margin-top: 1.5rem;
      background: #0a6847;
      color: white;
      padding: 0.6rem 1.2rem;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 500;
      transition: background 0.2s ease;
    }

    .btn:hover {
      background: #085c3f;
    }

    /* Print Styles */
    @media print {
      body {
        background: white;
      }
      .receipt-container {
        box-shadow: none;
        border: 1px solid #ccc;
      }
      .btn, .no-print {
        display: none;
      }
    }
  </style>
</head>
<body>
  <div class="receipt-container">
    <h1>✅ Booking Confirmed!</h1>
    <p>Thank you, <strong><%= user.name %></strong>!</p>

    <h3>Booking Details</h3>
    <div class="info">
      <p><strong>Court:</strong> <%= court.name %></p>
      <p><strong>Location:</strong> <%= court.location %></p>
      <p><strong>Date:</strong> <%= new Date(slot.date).toLocaleDateString() %></p>
      <p><strong>Time:</strong> <%= slot.startTime %> - <%= slot.endTime %></p>
      <p><strong>Price:</strong> $<%= slot.price %></p>
    </div>

    <hr class="divider">

    <div class="status">Status: Confirmed ✅</div>
    <div class="booking-id">Booking ID: <%= booking._id %></div>

    <div class="no-print">
      <a href="/booking" class="btn">View My Bookings</a>
      <button onclick="window.print()" class="btn" style="margin-left: 0.5rem;">🖨️ Print Receipt</button>
    </div>
  </div>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Footlinks - Home</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      display: flex;
      height: 100vh;
      background: #f8f9fa;
    }

    /* Sidebar */
    .sidebar {
      width: 200px;
      background: #0a6847;
      color: #fff;
      display: flex;
      flex-direction: column;
      padding: 1rem;
    }
    .sidebar h2 {
      margin: 0 0 2rem;
      text-align: center;
      font-size: 1.2rem;
      color: #fff;
    }
    .sidebar a {
      color: #fff;
      text-decoration: none;
      padding: 0.75rem 1rem;
      border-radius: 6px;
      margin-bottom: 0.5rem;
      transition: background 0.2s;
    }
    .sidebar a:hover {
      background: #085c3f;
    }

    /* Main content */
    .main-content {
      flex: 1;
      display: flex;
      flex-direction: column;
    }

    /* Topbar */
    .topbar {
      display: flex;
      justify-content: flex-end;
      align-items: center;
      padding: 1rem;
      background: #fff;
      border-bottom: 1px solid #ddd;
    }
    .login-circle {
      width: 40px;
      height: 40px;
      background: #0a6847;
      color: #fff;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: bold;
      text-decoration: none;
      transition: background 0.2s;
    }
    .login-circle:hover {
      background: #085c3f;
    }

    /* Content body */
    .content {
      padding: 2rem;
      flex: 1;
    }
    .content h1 {
      color: #0a6847;
      margin-bottom: 1rem;
    }
  </style>
</head>
<body>
  <!-- Sidebar -->
  <div class="sidebar">
    <h2>Footlinks</h2>
    <a href="/courts">Courts</a>
    <a href="/booking">Bookings</a>
  </div>

  <!-- Main -->
  <div class="main-content">
    <!-- Top bar -->
    <div class="topbar">
      <a href="/user/login" class="login-circle">L</a>
    </div>

    <!-- Body -->
    <div class="content">
      <h1>Welcome to Footlinks</h1>
      <p>Find and book your favorite football courts with ease.</p>
    </div>
  </div>
</body>
</html>
